import NotFound from '@/components/external/not-found';
import { Title } from '@/utils/components';

export default function NotFoundPage() {
  return (
    <>
      <Title title='NotFound' />
      <NotFound />
    </>
  );
}
